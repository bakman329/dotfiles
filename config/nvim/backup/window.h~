#ifndef WINDOW_H
#define WINDOW_H

#include <SDL2/SDL.h>
#include <iostream>

class Window {
 public:
    bool init(SDL_Window&, SDL_Surface&);
    void update();
    void cleanup();

 private:
    const int SCREEN_WIDTH = 640;
    const int SCREEN_HEIGHT = 480;
};

#endif
